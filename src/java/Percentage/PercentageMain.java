public class PercentageMain {
    public static void main(String[] args){
        Percentage percentage = new Percentage();
        percentage.setObtainedMarks(400);
        percentage.setTotalMarks(600);
        percentage.getPercantage();
        percentage.toDisplay();
    }
}
